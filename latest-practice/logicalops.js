/* let a = 15;
let b = 15;
if ((a == 10) && (b == 10)) {
    console.log('a value is lower then b');
} else if ((a <= 10) && (b == 15)) {
    console.log('b value higher then b');
} else {
    console.log('both are same');
} */
//============================//

/* let a = 3;
let b = -2;

console.log(a > 0 && b > 0);

console.log(a > 0 || b > 0);

console.log(!(a > 0 || b > 0));

console.log((a == 1) && (b == 1)); */
//============================//

/* let isVerified = true;
let isLoggedin = true;
let hasPaymentToken = true;
let isGuest = true;
if ((isVerified) && (isLoggedin) && (hasPaymentToken)) {
    console.log('Greating Message to user');
    console.log('Grant Access to paid cource')
} else if ((isVerified) && (isGuest)) {
    console.log('greeting Message to Guest');
    console.log('allow free previews');
} else {
    console.log('please contact custmer care survice agent');
}
 */
//============================//


/* let nameVinay = false;
if (nameVinay === true) {
    console.log('this is true');
} else if (nameVinay != true) {
    console.log('fake persone');
} else {
    console.log(this is not true');
     //============================//
} */